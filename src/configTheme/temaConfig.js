import { createMuiTheme, darken } from '@material-ui/core'

const theme = createMuiTheme({
    palette:{
        primary:{
            main:"#EDEDED"
        },
        secondary:{
            main:"#fffff"
        }
    }
})
export default theme
